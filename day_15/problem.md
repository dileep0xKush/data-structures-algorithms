## 🧩 Problem: *Square of a Number Without Using Built-in Power Function*

In a programming class, a student is asked to calculate the **square of a given number**, but there’s a rule:

> ❌ You are **not allowed to use built-in power functions** (like `pow()` or `**`).

You are given one integer:

* `num` → the given number

Your task is to:

> **Find the square of the number using basic operations** and print the result.

---

### ✔️ Input

One integer:

```
num
```

---

### ✔️ Output

Print:

```
Square of the number = <result>
```

---

### 🧪 Example

**Input**

```
6
```

**Output**

```
Square of the number = 36
```

---

### 💡 Hint (Optional)

You can solve this using:
✔️ Multiplication (`num × num`)

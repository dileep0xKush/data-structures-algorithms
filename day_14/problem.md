## 🧩 Problem: *Seat Exchange Without Extra Space*

In a classroom, two students are asked to **exchange their seat numbers**, but there’s a rule:

> ❌ You are **not allowed to use any extra (third) variable**.

You are given two integers:

* `seat1` → seat number of Student A
* `seat2` → seat number of Student B

Your task is to:

> **Swap the two seat numbers without using a third variable** and print the updated values.

---

### ✔️ Input

Two integers:

```
seat1
seat2
```

---

### ✔️ Output

After swapping, print:

```
After swapping:
Seat 1 = <new value>
Seat 2 = <new value>
```

---

### 🧪 Example

**Input**

```
10
20
```

**Output**

```
After swapping:
Seat 1 = 20
Seat 2 = 10
```

---

### 💡 Hint (Optional)

You can solve this using:
✔️ Arithmetic operations (`+` and `-`) **or**
✔️ Bitwise XOR (`^`)
